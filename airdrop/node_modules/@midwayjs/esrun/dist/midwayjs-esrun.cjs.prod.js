'use strict'

Object.defineProperty(exports, '__esModule', { value: true })

var readDefaultTsconfig = require('@swc-node/register/read-default-tsconfig')
var register$1 = require('@swc-node/register/register')

function register() {
  const options = readDefaultTsconfig.readDefaultTsConfig()

  // ensure all files are compiled
  options.files = null
  // transpile to CommonJS by Default
  options.module = 'CommonJS'

  register$1.register(options)

  if (options.paths && Object.keys(options.paths).length) {
    require('tsconfig-paths/register')
  }
}

exports.register = register
